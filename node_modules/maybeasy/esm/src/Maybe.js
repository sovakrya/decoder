/**
 * Maybe describes a value that is optional. It is safer to use then null and
 * undefined.
 */
export class Maybe {
    /**
     * An alias for `map`
     */
    and(fn) {
        return this.map(fn);
    }
}
export default Maybe;
